up

curl -s `up url`
# contains: Static site example

curl -s `up url`
# contains: <link rel="stylesheet" href="style.css">

curl -s `up url`
# contains: analytics.load("API KEY HERE");

curl -s `up url`
# contains: <script>const config = {

curl -s `up url`
# contains:  <script src="src/app.js"></script>
